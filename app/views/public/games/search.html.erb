<div class="container">
  <div class="row mt-3 text-center">
    <div class="col-md-12">
      <h1>製品名を入力して検索</h1>
    </div>
  </div>
  <div class="row mt-3 text-center">
    <div class="col-md-12">
      <%= form_tag(games_search_path, method: :get) do %>
        <%= text_field_tag :title, @title %>
        <%= button_tag "検索" %>
      <% end %>
    </div>
  </div>
  <div class="row mt-3">
    <% if @games %>
      <% @games.first(10).each do |game| %>
      <div class="col-sm-4 text-md-right">
        <%= image_tag game.largeImageUrl %>
      </div>
      <div class="col-sm-8">
        <h5><%= link_to game.title, new_review_path %></h5>
        <p>発売元: <%= game.label %></p>
        <p>対応機種: <%= game.hardware %></p>
        <p>発売日: <%= game.salesDate %></p>
      </div>
      <% end %>
    <% else %>
    　検索結果は見つかりませんでした
    <% end %>

  </div>
</div>